apiVersion : apps/v1
kind: Deployment
metadata : 
name: nodeapp-deploymentt
labels:
app: nodeapp
spec:
replicas: 2

selector:
matchLabels :
app: nodeapp
template:
metadata:
labels:
app: nodeapp

spec:
containers:
- name: nodeserver
image: sjrepository/sjimage2:latest
ports: 
- containerRoot: 1000
